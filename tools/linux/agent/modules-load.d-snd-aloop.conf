# Shipped at /etc/modules-load.d/snd-aloop.conf
# Auto-load the ALSA loopback module so appsandbox-audio can capture
# the system audio output at the kernel layer (no PipeWire/PulseAudio
# in the hot path).
snd-aloop
